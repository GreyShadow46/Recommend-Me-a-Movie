# Session security (use a long random string)
SESSION_SECRET=your_random_session_secret

# MySQL Database configuration (Default values included change if needed)
DB_HOST=db
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=recommend_me_a_movie
DB_ROOT_PASSWORD=your_root_password

# Encryption key for sensitive data (long random string)
ENCRYPTION_KEY=your_encryption_key

# Email configuration (required for automated emails)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_app_password

# Environment
NODE_ENV=production